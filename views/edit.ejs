<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.tailwindcss.com"></script>
    <title>Profile</title>
</head>
<body class="w-full h-screen bg-zinc-900 text-white p-10">
    <div class="">
        <div class="flex justify-end w-full">
            <a class="px-3 py-2 text-sm inline-block bg-red-500 rounded-md mb-2" href="/logout">Logout</a>
        </div>
        
        <h5 class="mb-3">Edit post here</h5>
        <form action="/update/<%= post._id%>" method="post">
            <textarea placeholder="Whats on your mind" class="outline-none p-3 w-1/3 resize-none bg-transparent border-2 border-zinc-700 rounded-md block" name="content"  ><%= post.content%></textarea>
            <input type="submit" value="Confirm Edit" class="px-3 py-2 w-40 rounded-md bg-yellow-500 hover:bg-blue-600 text-white block mt-2 ">
        </form>
        
    </div>
</body>
</html>